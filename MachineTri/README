Toutes les classes ont étés écrites, y compris les classes de tests. La documentation a également
été rédigée.

Compilation :
On peut compiler tout le projet. Il reste cependant des erreurs : dans le destructeur de CMachine, le code plante.
Le nombre aléatoire pour l'assignation des catégories est toujours le même.
